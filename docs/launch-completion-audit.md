# Launch Completion Audit

Last updated: 2026-06-21

Objective: make Autobazar123 safely launch-ready, with evidence, then prepare the first real ad-supply push.

Completion decision: not complete. The main deploy/migration/payment-success/payment-failure/cron-route/scheduled-cron/maintenance-bypass/local-RLS blockers are now verified green. Production release gauntlet is 17/18, but public launch is blocked by missing real Turnstile envs for buyer inquiries and explicit owner approval before public SEO indexing.

## Success Criteria

- Production stays crawler-blocked/noindex until launch checks pass and the user explicitly agrees to open it. If maintenance mode is re-enabled, it stays enabled until the same explicit opening decision.
- Dirty working tree is classified before shipping.
- `docs/launch-checklist.md` truthfully marks done, partial, blocked, and not-in-scope launch items.
- Core flows have evidence: homepage, search/results, listing detail, signup, login, reset, add/edit/manage listing, inquiry/contact, payment if enabled, admin/dealer permissions, maintenance bypass, `/api/health`.
- Verified blockers receive root-cause fixes only.
- Preview is the primary validation target; Production also needs a compatible-code smoke before remote RLS hardening, then final smoke while crawler indexing remains disabled.
- Cheapest acceptable brands/models plan is decided and documented.
- Dealer/free-ad-upload push is prepared, but not executed until the site is safe to open.
- `PROJECT_STATUS.md` stays current after important verified changes.

## Prompt To Artifact Checklist

| Requirement | Evidence | Status | Gap |
| --- | --- | --- | --- |
| Reconcile dirty repo state | `PROJECT_STATUS.md` classifies the current main dirty tree as the separate taxonomy lane. Reviewed app source `2297260` was deployed from clean worktree `C:\Users\User\Desktop\Projects\ab123-rs-153336`; remote migrations had already been applied from reviewed source `a2417f3`. | Partial | Dirty taxonomy work remains separate and should not be mixed into launch deploy/migration work. |
| Preserve unrelated user work | `git status --short` still shows the taxonomy discovery/promotion files in main, while the reviewed launch source is clean. | Partial | Needs review again before any commit/push. |
| Use launch checklist | `docs/launch-checklist.md` updated with core-flow statuses and evidence. | Done | Keep updating as real-account and deploy checks run. |
| Homepage | Local smoke/UI/webapp audit evidence in `docs/launch-checklist.md`; 2026-05-16 UI gates passed after fixing redesigned quick-choice cards that were wider than a 320px viewport. Fresh protected Preview route smoke and Production smoke both include the homepage route. | Verified local and remote route smoke | Needs user visual review before public opening. |
| Search/results | Local route/UI checks, mocked Algolia release-gauntlet evidence, stale top-ad optional filter removal, read-only real Algolia/Supabase count check, focused desktop/mobile `/vysledky?bodyStyle=motorcycle` runtime check, and full local `npm run audit:webapp` evidence across desktop/mobile results and detail routes. | Partial | Preview browser validation against deployed Algolia env is still missing. |
| SEO/crawl/indexing | pSEO launch gating derives sitemap brand/model URLs from active inventory, requires at least 10 active matching ads before city pSEO URLs enter the sitemap, noindexes/404s below-threshold city pages, removes hardcoded internal city pSEO links, and public copy no longer claims broad scale/verified marketplace status. Fresh postdeploy Production SEO fetch shows homepage canonical `https://www.autobazar123.sk`, `/sitemap.xml` with 128 `www` URLs, `/llms.txt` primary URLs on `www`, `/api/health` healthy, and intentional crawler blocking via noindex headers/meta plus `/robots.txt` `Disallow: /`. | Partial | `www` canonical/sitemap/llms are deployed, but indexing must stay off until all remaining launch gates pass and the owner explicitly approves public SEO opening. |
| Algolia search checker quality | `npm run test:algolia-search-script` covers sample-hit required fields, no-active-ad failure, and count mismatch failure offline; `npm run check:algolia-search` verifies current live read-only state. | Verified local | Does not replace preview browser validation against deployed Algolia env. |
| Listing detail | Local webapp audit over sampled detail URLs, 2026-06-20 launch screenshot pass over a real active detail page, and `src/lib/cars/public-car-detail.ts` now fetches public detail through a server-only admin helper with active/visible filters so remote profile RLS hardening will not break the page. | Verified local | Deploy compatible code before applying remote RLS hardening, then preview/prod smoke after deploy approval. |
| Live Supabase profile/dealer RLS | Remote launch migrations were applied from reviewed source `a2417f3` while excluding `20260619214332_add_vehicle_taxonomy_metadata.sql`. Fresh `npm run check:live-rls-posture -- --json` returned `ok=true`, 4 safe probes, 0 leaked rows, and 0 probe errors for `profiles.email`, `profiles.phone`, `profiles.credit_balance`, and raw `dealers`. Fresh local Docker-backed `npm run test:db:rls` from clean reviewed source `2297260` passed 2 files / 26 pgTAP tests. | Verified local and remote | Rerun after any future DB/RLS migration change. |
| Launch screenshots and local visual sanity | 2026-06-20 launch screenshot pass captured 18 desktop/mobile screenshots across homepage, results, motorcycle results, real detail, seller dashboard, seller create tab, seller edit page, dealer dashboard, and payment success. Report recorded 0 failed statuses, console messages, page errors, network failures, horizontal-scroll issues, or too-wide elements. Later protected Preview and Production route smoke passed. Production release gauntlet passed 17/18 browser checks. | Verified local plus remote route/browser smoke | Needs user visual review; buyer inquiry remains blocked until Turnstile envs are configured and redeployed. |
| Local and remote launch route smoke | 2026-06-21 `npm run test:smoke` now checks `/api/health`, `/`, `/vysledky`, `/auth/login`, `/site-map`, `/sitemap.xml`, `/robots.txt`, `/llms.txt`, `/platba/uspech?session_id=cs_test_release_gauntlet`, and one real `/auto/...` URL discovered from `/sitemap.xml`. Fresh Production smoke passed 10/10 against `https://www.autobazar123.sk`. Fresh protected Preview route smoke passed through the Vercel share cookie for `/`, `/api/health`, `/vysledky`, `/auth/login`, `/pridat-inzerat`, `/moj-ucet`, `/dealer`, `/admin`, `/site-map`, and `/robots.txt`. | Verified local and remote route smoke | Broader deployed authenticated browser journeys remain open. |
| Local performance budget | Production-mode local audit now passes. The first production-mode run caught a real CSP issue from Zod browser JIT, fixed in `src/app/layout.tsx` by enabling Zod `jitless` mode before hydration without weakening CSP. The audit runner now scopes synthetic `x-forwarded-for` headers to local app requests only, preventing test rate-limit exhaustion without leaking that header to Algolia. Final command passed: `PLAYWRIGHT_CHROMIUM_CHANNEL=chrome PLAYWRIGHT_REUSE_SERVER=true TEST_URL=http://localhost:3000 WEBAPP_AUDIT_MODE=production npx playwright test tests/webapp-audit.ts --project=desktop-chromium --reporter=line`, 6/6 tests, 80/80 route/viewport checks, 0 failing routes, and 0 DevTools issues. `npm run check:performance-budgets` passed with p95 JS transfer `2258037`, p95 main-thread work `363.1`, and p95 DOMContentLoaded `691.65`. | Verified local | Preview/external performance audit still belongs after deploy approval, but local performance is no longer a launch-blocker lane. |
| Production route bundle budget | `npm run test:production-bundle-budget-script` passed 7/7, and fresh `npm run check:production-bundle-budget` passed from current `.next` artifacts with 33 routes, max first-load uncompressed JS 1,646,120 bytes, p95 first-load uncompressed JS 1,569,581 bytes, total static JS/CSS 3,449,624 bytes, and 56 static JS/CSS files. The launch rollup now runs this after `npm run build`. | Verified local | This supports the diagnosis that the 8.9 MB browser JS number is development-audit inflated, but it does not replace a production/external browser performance audit. |
| Vercel env metadata names | `npm run test:vercel-env-names-script` passed 8/8 after adding Turnstile to the required launch env names. Real `npm run check:vercel-env-names` now blocks without pulling or printing secrets because Preview and Production are missing `NEXT_PUBLIC_TURNSTILE_SITE_KEY` and `TURNSTILE_SECRET_KEY`. | Verified metadata blocker | Add real Turnstile envs, redeploy so the public key is in the client bundle, then rerun deployed inquiry/browser smoke. |
| Vercel Upstash env value/sensitive metadata | `npm run test:vercel-env-values-script` passed 12/12 and now guards the pinned modern CLI path plus the boundary that Vercel sensitive tokens are not treated as pull-readable values. Real `npm run check:vercel-env-values` pulls Preview/Production env snapshots into an OS temp folder, checks the pull-readable Upstash URL value, verifies `UPSTASH_REDIS_REST_TOKEN` exists in metadata with `type=sensitive`, deletes temp files, and prints no secret values. | Verified local metadata/pull | Cloud runtime/provider smoke is still required before trusting Preview/Production rate-limit behavior. |
| Signup | Page/UI checks plus mocked register/resend POST route coverage. Real Resend/Gmail delivery of `Potvrdenie registrácie - Autobazar123` passed on 2026-06-20. A RED browser check first proved raw Supabase action links confirmed the user but landed on `/auth/auth-code-error`; signup/resend emails now use app `token_hash&type=email` callback URLs. Real browser signup submit, emailed confirmation-link login to `/moj-ucet`, 0 console/page errors, and temp-user cleanup passed. | Verified local, partly manual/artifact-backed | The full Gmail/browser flow is recorded as narrative evidence, not a single repeatable command. Needs preview/prod validation after deploy approval. |
| Login | Release-gauntlet and focused E2E auth entry/exit passed with configured account. | Verified local | Needs preview/prod validation after deploy approval. |
| Password reset and account password change | Page/UI checks plus mocked password-reset POST route coverage for recovery link generation and queueing. Account password recovery POST route coverage verifies token verification, admin password update, service-role recovery-session revocation, benign consumed-session cleanup, and failure handling. The 2026-06-19 release gauntlet verifies a real Supabase recovery token in the browser, old password rejection, temporary-password login, and password restoration. Real Resend/Gmail delivery of `Obnovenie hesla - Autobazar123` passed on 2026-06-20. Real emailed reset-link password update, old-password rejection, new-password acceptance, and browser login to `/moj-ucet` passed with 0 console/page errors. Authenticated password-change route coverage verifies auth, payload validation, password update failure, success, and other-session revocation. | Verified local, partly manual/artifact-backed | The full Gmail/browser flow is recorded as narrative evidence, not a single repeatable command. Needs preview/prod validation after deploy approval. |
| Add listing | Route tests cover draft create, free auto-publish, and failed publish cleanup. The 2026-06-19 release gauntlet verifies authenticated seller browser creation with two uploaded photos and cleanup. | Verified local | Needs preview/prod validation after deploy approval. |
| Image upload URL | `npm run test:uploads-release` passed 3 files / 18 tests, covering `/api/images/upload-url`, upload client behavior, and CSP for Cloudflare direct uploads. The route now has direct coverage for CSRF rejection, auth requirement, missing Cloudflare config fail-closed behavior, upload rate limiting, Cloudflare direct-upload success, malformed Cloudflare success payloads failing closed, provider rejection with generic response, and thrown provider/network failures without response/log leakage of raw token-bearing errors. | Verified local | Needs preview/prod validation after deploy approval because live Cloudflare credentials and direct upload behavior can only be proved in deployed runtime. |
| Edit/manage listing | Route tests cover quick edit, feature actions, seller-owned delete with Algolia cleanup, and ownership denial. The 2026-06-19 release gauntlet verifies owner edit, photo removal, mark-sold, seller dashboard delete/remove, non-owner edit-page denial, and cleanup. | Verified local | Needs preview/prod validation after deploy approval. |
| Inquiry/contact | Contact and inquiry route tests cover validation, rate-limit/config failure, auth, captcha, recipient ownership, self-message rejection, and handoff. The 2026-06-20 release-gauntlet inquiry check verifies a non-admin buyer submits from a real listing detail page, the DB row has the expected ad/sender/recipient/message, the seller sees it in `/moj-ucet?tab=messages`, and cleanup leaves 0 `Release gauntlet inquiry` rows. | Verified local | Needs preview/prod validation after deploy approval. |
| Payment if enabled | Checkout-status route tests cover authenticated actor lookup, dealer-owner fallback lookup, pending response, and lookup failure. Stripe checkout/webhook tests cover ownership, metadata, idempotency, fail-closed storage, confirmation/failure email queueing, duplicate/paid/unpaid behavior, and webhook retry behavior. Real Preview success smoke completed a seller `prolong_top` Checkout with test card `4242 4242 4242 4242`, returned to `/platba/uspech`, reached paid checkout state, created a billing transaction, wrote `payment_notifications.billing_transaction_id`, sent payment confirmation email delivery, recorded a processed webhook log with session/user context, then cleaned up all run rows and restored the seller ad. The first failed-payment smoke exposed missing PaymentIntent metadata/receipt-email plus missing Stripe failure-event subscription; `2297260` fixed metadata/receipt email, the Stripe test webhook endpoint now listens for failure events, and the rerun `preview-failed-1782062940623` proved failed checkout state, no billing transaction, unchanged ad, sent failure email, processed Production `payment_intent.payment_failed` webhook, and 0 cleanup residue. | Verified remote for seller listing success/failure | Broader deployed dealer topup variants remain open. |
| Remote launch migration | Historical clean-worktree dry-runs repeatedly proved only the three launch-critical migrations would apply. Later 2026-06-21 status: the remote migration batch was applied from the reviewed worktree and the blocked taxonomy metadata migration stayed out of the launch lane. Fresh live RLS and payment-notification checks now prove the two remote effects that depended on that batch. | Verified remote | Keep using a clean reviewed source for future DB pushes; never run plain `db push --include-all` from dirty main while the taxonomy metadata lane exists. |
| Launch migration worktree guard | `npm run check:launch-migration-worktree -- --root <clean-worktree>` checks a chosen worktree for required launch migration files, full worktree cleanliness, dirty `supabase/migrations` entries, and the unrelated taxonomy metadata migration. Fresh 2026-06-21 continuation `npm run test:launch-migration-worktree-script` passed 7/7; the guard fails as expected in the current dirty main worktree because `20260619214332_add_vehicle_taxonomy_metadata.sql` is present and the worktree has uncommitted launch-hardening changes. Fresh Supabase CLI capability recheck on 2026-06-21 showed CLI `2.107.0`; `db push --help` still has no per-migration-file selection flag; and the Supabase changelog scan found no relevant stable db-push selection capability. | Verified local | Use it before Supabase dry-runs or remote DB pushes; it does not replace `npx supabase migration list` or `db push --dry-run --include-all`. |
| Launch blocker aggregate | `npm run check:launch-blockers` runs the shorter non-mutating blocker checks; `npm run check:launch-blockers:full` also runs the pinned Vercel Preview build preflight. Clean reviewed worktree `2297260` passed `npm run check:deploy-source-readiness`, `npm run test:payments-release` 6 files / 53 tests, `npm run check:launch-blockers:full -- --allow-extra-worktrees`, and later `npm run test:db:rls` 2 files / 26 pgTAP tests; remote evidence also proved Preview deploy, Production deploy, live RLS, Production smoke, protected Preview route smoke, Stripe success/failure, cron route smoke, and maintenance bypass. Fresh current-main non-mutating rollup blocks only on expected current-source gates: dirty deploy source, missing Turnstile Vercel env names, and current-tree launch migration safety from the separate taxonomy lane; the fresh full variant also passes the pinned Vercel Preview local build preflight. | Verified current reviewed source and current dirty main posture | Rerun after any future launch source change. |
| Deploy source preflight | `npm run check:deploy-source-readiness` is an executable gate for git source state, `.vercel/project.json`, and root `.vercelignore`. The clean reviewed worktree was updated to `2297260` after the failed-payment fix and is clean. Direct checks keep the deferred taxonomy/provider scripts/helpers and `20260619214332_add_vehicle_taxonomy_metadata.sql` out of that source. | Verified clean reviewed source | Dirty main still must not be used for deploy/migration unless explicitly accepted and reverified. |
| Owner approval packet | `docs/launch-remote-migration-deploy-runbook.md` now has a compact approval packet for the four separate manual gates: Preview deploy, Production compatible-code deploy with indexing disabled, exact remote Supabase migration apply, and public SEO indexing. Each gate lists the approval wording, command, stop conditions, and evidence to record. | Verified docs | This packet does not replace actually getting owner approval or running the deploy/migration/smoke sequence. |
| Turnstile env gate | Production browser smoke proved buyer inquiry is blocked when Turnstile is missing. `scripts/check-vercel-env-names.mjs` now requires `NEXT_PUBLIC_TURNSTILE_SITE_KEY` and `TURNSTILE_SECRET_KEY`; `npm run test:vercel-env-names-script` passes 8/8, and real `npm run check:vercel-env-names` now blocks on those missing names in Preview and Production without printing values. Turnstile Siteverify handling now checks successful responses against expected `action` and request hostname and fails closed for missing fields in production. | Verified local and Vercel metadata | Add real Turnstile keys, redeploy so the public key is in the client bundle, then rerun deployed inquiry/browser smoke. |
| Release gate script tracking | `.gitignore` now unignores package-referenced launch support scripts/tests. The launch blocker rollup has RED/GREEN-tested package-script tracking so missing, gitignored, or untracked package-referenced local files become a blocker automatically. A later correction fixed adjacent-path detection so the checker no longer skips neighboring package paths such as `src/lib/cron/route-helpers.test.ts`. Approved launch-support package-referenced files were reviewed and staged. Deferred taxonomy/provider package shortcuts were removed from `package.json`; keep that separate lane not launch-approved until the owner explicitly approves it. `scripts/import-jato-taxonomy.ts` now requires explicit `--write` and rejects unsupported `--dry-run`; a real read-only import planner remains future work before exposing a package shortcut. The previously ignored i18n diacritics test now passes and protects explicit manual-dictionary suppressions such as `chyba -> chyba`. | Verified local | Recheck package-script tracking before commit/push/deploy. |
| Clean launch worktree local gate | Removed historical path `C:\Users\User\Desktop\Projects\autobazar123-launch-db` passed `npm run easy:quick`, `npm run test:security:release-gate`, `npm run test:db:rls`, `npm run build`, `npm run check:launch-test-coverage -- --require-complete`, `npm run check:algolia-search`, and `npm audit --json`. Build generated 331 pages; launch coverage is complete; Algolia has 56/56 active ads searchable; audit has 0 vulnerabilities. | Verified local | This is historical evidence only, not a current deploy source; preview deploy, production deploy, remote migrations, live anon probe, payment notification logging recheck, and preview/prod payment smoke remain open. |
| Vercel Preview build/deploy | Historical app-side blockers were fixed by moving public dealer pages and mutable pricing/taxonomy API routes behind `connection()` request boundaries. The remaining static-PPR packaging blocker was cleared on 2026-06-21 by turning off global Cache Components/PPR, keeping featured-cars caching via `unstable_cache`, removing the anonymous Supabase client's custom Next fetch wrapper, deriving sitemap pSEO URLs from active ad rows joined to canonical brand/model slugs, and ignoring generated `.vercel/**` files in ESLint. Current Preview deployment `dpl_8mpqjPYXKpYNkuXicZ6YUghDGkad` and Production deployment `dpl_CSYeS3gn1VYRkCz2LGdkt73hiNNN` reached Ready from reviewed source `2297260`; protected Preview route smoke and Production route smoke passed. | Verified local preflight and remote deploy | Broader authenticated deployed browser flows remain open. |
| Admin/dealer permissions | Admin-positive, non-admin admin denial, non-dealer prompt, dealer topup payload, seller paid-listing payload, and admin dealer-verification request visibility pass locally; dealer verification request API has route coverage for authenticated owner-scoped reads, duplicate pending guard, verified/missing dealer rejection, and request creation; Playwright loads `.env.local`; release gauntlet now supports separate admin, non-admin, seller, and dealer credentials; read-only launch coverage checker confirms complete launch account coverage. | Verified local | Needs preview/prod validation after deploy approval. |
| Launch test account setup | `docs/launch-test-accounts.md` documents current candidate counts, required E2E env keys, safe rules, and verification commands; `npm run check:launch-test-coverage -- --require-complete` reports complete local coverage for primary/admin, non-admin, seller-with-owned-ad, and dealer coverage. | Verified local | Keep secrets out of logs and recheck before preview/prod smoke. |
| Launch account checker quality | `npm run test:launch-test-coverage-script` covers role-specific fallback and DB candidate-count logic offline; `npm run check:launch-test-coverage` verifies current live read-only state. | Verified local | Does not replace real preview/prod auth smoke. |
| Cron/fallback reliability | `expire-ads` now returns explicit degraded non-success responses when expired-ad DB updates fail or Algolia stale-record cleanup fails; Algolia cleanup failure records governed critical fallback `cron.expire_ads_algolia_cleanup_failed`. `send-alerts` now returns degraded `502` when saved-ad or saved-search email delivery fails or throws, uses deterministic Resend idempotency keys, conditionally claims saved-ad and saved-search rows before sending, skips rows already claimed by overlapping cron invocations, and rolls state back when provider delivery fails or the sender throws. `process-email-jobs` now returns degraded `502` when queued email processing reports failed or requeued jobs. Direct email processor tests prove DB state-update failures are no longer counted as handled. Deployed Production smoke proved unauthorized requests return 401 and authorized `expire-ads`, `cleanup-sold`, `process-email-jobs`, and `send-alerts` return 200; `expire-ads` removed 136 non-public Algolia candidates and follow-up Algolia parity was 56/56. Vercel Production runtime logs later proved the scheduled `GET /api/cron/cleanup-sold` ran at 18:56:21 UTC with HTTP 200 inside the 18:00-18:59 UTC window, and follow-up Algolia parity was still 56/56. Official Vercel Cron docs were rechecked on 2026-06-21. | Verified routes and first scheduled invocation | Resend keys expire after 24 hours, so provider/idempotency behavior still needs observation over time under real scheduled runs. |
| Maintenance bypass | Local token helper, unlock route, and proxy host behavior are covered by unit tests. The unlock route no longer accepts the legacy `MAINTENANCE_PASSWORD` env alias. Production runtime smoke verified: homepage redirected to `/maintenance` when `maintenance_mode=true`, unlock returned 200 and set `maintenance_bypass`, homepage with bypass returned 200, and `maintenance_mode` was restored to `false` with homepage 200. | Verified remote | Re-smoke after any maintenance env/proxy change. |
| `/api/health` | `npx vitest run src/app/api/health/route.test.ts src/proxy.test.ts src/lib/security/maintenance-bypass.test.ts` passed 29/29 tests, covering local health route behavior plus proxy/maintenance-bypass behavior. Fresh Production smoke and protected Preview route smoke both included `/api/health` with HTTP 200. | Verified local and remote route smoke | Keep in every postdeploy smoke. |
| Fix only verified blockers | Current changes are targeted to verified analytics, search, route, test, and checklist gaps. | Partial | Continue this rule for remaining real-flow issues. |
| Dependency posture | `npm audit --json`, `npm run build`, `npm run typecheck`, `npm run easy:quick`, and `npm run test:security:release-gate` pass after direct dependency bumps, explicit transitive overrides, the latest homepage reflow fix, stale `.next/dev` cleanup before build/typecheck, moving test helpers out of App Router route exports, and updating the release policy for moved quality-gate OIDC internals. | Verified local | Needs preview/prod deploy validation before shipping. |
| Task 11 release gate | On 2026-06-20, core local release commands passed. Later, reviewed source `a2417f3` was deployed and migrations were applied. Current reviewed source `2297260` was deployed to Preview/Production; live RLS passed; local Docker-backed RLS passed; Production smoke passed 10/10; protected Preview route smoke passed; Preview Stripe success and failure smokes passed with notification/email/logging cleanup; deployed cron route smoke passed; scheduled `cleanup-sold` cron log passed; deployed maintenance-bypass smoke passed; full clean-worktree blocker rollup passed; and Production release gauntlet passed 17/18 browser checks. | Verified through deploy/migration/payment/cron/maintenance/RLS/browser path | Buyer inquiry is blocked by missing Turnstile envs; public SEO approval remains open. |
| Task 11 local smoke/lint/build guard | On 2026-06-21, `npx tsx --test scripts/check-local-launch-smoke-core.test.ts` passed 4/4, `npx vitest run src/app/llms.txt/route.test.ts` passed 2/2, `TEST_URL=http://localhost:3000 npm run test:smoke` passed 10/10, `npm run build` passed, and `npm run check:launch-blockers` now includes the non-server smoke target gate plus lint, typecheck, and local production build gates after fixing the `/llms.txt` route conflict. | Verified local | Does not replace preview/prod smoke. |
| Preview as main validation target | Preview was deployed first from the reviewed source and smoked before/around the Production and migration sequence. Production was then deployed before remote RLS hardening, matching the shared-database safety rule. | Done for current sequence | Keep using Preview first for future launch changes. |
| Brands/models launch posture | `docs/ad-supply-launch-plan.md` chooses current taxonomy plus manual normalization; no paid provider for launch without owner approval. | Done | User still needs to accept the launch stopgap. |
| VIN and always-updated taxonomy capability | `docs/product-capability-backlog.md` records the existing scaffolding and the provider, cost/license, sync, migration, monitoring, rollback, and preview work still required. | Open backlog | Do not describe either capability as complete or enable live sync without owner approval. |
| Dirty taxonomy/discovery lane | Current dirty worktree adds external taxonomy discovery scripts, candidate promotion, active-only public taxonomy reads, and local-only migration `20260619214332_add_vehicle_taxonomy_metadata.sql`. Focused taxonomy tests now pass 7 files / 18 tests, including active-only public taxonomy read coverage, the write guard, and real read-only approved-candidate promotion dry-run coverage. The dry-run also covers existing inactive taxonomy rows so preview counts match the real write path. No-flag smoke checks for Wikidata, Autobazar.eu, Otomoto, Mobile.de, and promotion stop before mutation with explicit errors; promotion `--dry-run` ran read-only and reported 0 approved candidates, 0 brand inserts, and 0 model inserts; `git diff --check`, `npm run typecheck`, and `npm run lint` pass. | Verified local, not launch-approved | Keep this lane out of launch-critical remote DB push unless the owner explicitly approves shipping the taxonomy metadata feature with launch. |
| Dealer/free-upload plan | `docs/ad-supply-launch-plan.md` drafts offer, rules, intake fields, message, and success target. | Prepared | Do not contact dealers until launch checks pass and user agrees to open. |
| PROJECT_STATUS updates | `PROJECT_STATUS.md` updated with evidence, blockers, and next tasks. | Done | Keep current after the next important change. |

## Verified Commands From Current Local Evidence

- `npm audit --json`: passed with 0 vulnerabilities.
- `npm run test:launch-blockers-script`: passed 16/16.
- `npm run test:performance-budget-gate-script`: passed 5/5.
- `npm run test:production-bundle-budget-script`: passed 7/7.
- `npm run check:production-bundle-budget`: passed with 33 routes, max first-load uncompressed JS 1,646,120 bytes, p95 first-load uncompressed JS 1,569,581 bytes, total static JS/CSS 3,449,624 bytes, and 56 static JS/CSS files.
- `npm run test:vercel-env-names-script`: passed 8/8 after Turnstile envs were added to the required metadata names.
- `npm run check:vercel-env-names`: blocked as expected; Preview and Production are missing `NEXT_PUBLIC_TURNSTILE_SITE_KEY` and `TURNSTILE_SECRET_KEY`, and no secret values were pulled or printed.
- `npx vitest run src/lib/security/turnstile.test.ts src/app/api/inquiries/route.test.ts src/app/api/listing-reports/route.test.ts`: passed 3 files / 17 tests after adding Turnstile action/hostname response checks.
- `npm run test:vercel-env-values-script`: passed 12/12.
- `npm run check:vercel-env-values`: passed; Preview and Production each have a non-empty pull-readable `UPSTASH_REDIS_REST_URL`, `UPSTASH_REDIS_REST_TOKEN` exists as sensitive metadata, temp files were deleted, and runtime smoke is still required.
- `npx vercel env ls preview` / `npx vercel env ls production`: passed and showed expected critical env names exist in both targets; values are not printed.
- `vercel env run` present/missing probes for Preview and Production: passed without printing values and showed sensitive values are not available to local CLI commands, so cloud runtime smoke/provider confirmation remains required.
- `PLAYWRIGHT_CHROMIUM_CHANNEL=chrome PLAYWRIGHT_REUSE_SERVER=true TEST_URL=http://localhost:3000 WEBAPP_AUDIT_MODE=production npx playwright test tests/webapp-audit.ts --project=desktop-chromium --reporter=line`: passed 6/6 with 80/80 route/viewport checks, 0 failing routes, and 0 DevTools issues.
- `npm run check:performance-budgets`: passed with production-mode audit data; p95 JS transfer `2258037`, p95 main-thread work `363.1`, and p95 DOMContentLoaded `691.65`.
- `npm run test:security:release-gate`: exited 0 with `SECURITY RELEASE GATE: OK`; unrelated PowerShell profile/Terminal-Icons warnings still print after the success line.
- `npm audit --json`: passed with 0 vulnerabilities across 1069 dependencies.
- `npm run test:payments-release`: passed 6 files / 52 tests.
- `npm run test:cron-email-release`: passed 10 files / 38 tests.
- `npm run list:fallbacks`: passed with 9 registered fallbacks.
- `npm run test:seo-taxonomy`: passed 3 files / 30 tests.
- `npm run check:launch-blockers -- --allow-extra-worktrees`: blocked as expected; branch/worktree cleanup with explicit review-worktree mode, package-referenced script file tracking with 111 local paths, tracked/untracked cleanup/refactor marker posture, working-tree whitespace/conflict-marker check, launch coverage, security release gate, dependency security audit, payment checkout/webhook release suite, cron/email release suite, fallback registry governance, Algolia parity, SEO/GEO local gate, local launch smoke target coverage, image upload release suite, local performance budget, local lint/typecheck, local production build, production route bundle budget diagnostic, Vercel Upstash value/sensitive metadata, live RLS, and Vercel static-PPR packaging passed, while deploy-source readiness, missing Turnstile Vercel env names, and current-tree migration safety remain blockers.
- `npm run check:launch-blockers:full`: earlier full run was blocked only on live anon RLS and current-tree migration safety and its added Vercel Preview local build preflight lane passed with `vercel@54.14.5`; rerun it from the chosen reviewed deploy source because the newer deploy-source readiness lane now supersedes that full-run source evidence. If the chosen source is an extra git worktree alongside the main repo, use `npm run check:launch-blockers:full -- --allow-extra-worktrees`.
- `npm run test:vercel-env-names-script`: passed 8/8 after release scripts were unignored and Turnstile envs were added to the required launch metadata names.
- `npm run test:vercel-env-values-script`: passed 12/12 after release scripts were unignored.
- `npm run test:production-bundle-budget-script`: passed 7/7 after release scripts were unignored.
- `npm run test:launch-blockers-script`: passed 13/13 after package-script tracking was added and release scripts were unignored.
- `npm run test:i18n-contract-script`: passed 4/4 after i18n script tests were unignored.
- `npm run test:i18n-diacritics-script`: passed 8/8 after making explicit same-word manual dictionary suppressions an intentional behavior.
- `npm run check:i18n-diacritics`, `npm run check:i18n-contract`, `npm run check:text-encoding`, and `npm run lint`: passed after the ignored-script cleanup.
- `npm run test:uploads-release`: passed 3 files / 18 tests after adding direct upload URL route coverage, malformed Cloudflare success payload fail-closed coverage, and hardening thrown provider failures to return/log generic errors.
- Earlier `npm run check:launch-blockers` and `npm run check:launch-blockers:full` runs after malformed upload-response hardening were blocked only on live anon RLS and current-tree migration safety; those results are superseded by the later untracked package-file guard.
- Fresh `npm run check:launch-blockers -- --allow-extra-worktrees`: blocked as expected with three blocker lanes: deploy-source readiness, missing Turnstile Vercel env names, and current-tree migration safety. Package-script tracking passed with 111 local file paths after adjacent-path detection was fixed, approved launch-support files were staged, deferred taxonomy/provider package shortcuts were removed, the deploy-source checker scripts were added, and root `.vercelignore` was added with launch-risk exclusions. Passing lanes included payments 6 files / 53 tests, cron/email 10 files / 38 tests, SEO/GEO 3 files / 31 tests, image upload release suite 3 files / 19 tests, performance, lint, typecheck, build, Vercel Upstash env value/metadata checks, live RLS, and Vercel static-PPR packaging. No deploy, push, email send, payment action, or remote DB write was run.
- Fresh live read-only smoke: `TEST_URL=https://www.autobazar123.sk npm run test:smoke` passed 10/10; a small installed-Chrome Playwright check loaded the live homepage with status 200, 0 console warnings/errors, 0 page errors, noindex headers/meta, healthy `/api/health`, and `/robots.txt` disallow. `npm run test:agent-browser` timed out and was stopped, so no success is claimed for that helper.
- 2026-06-20 Task 11 local gate:
  - `npm run easy:quick`: passed; lint, text/i18n/theme checks, `npx tsc --noEmit`, and unit tests passed, 105 files / 508 tests.
  - `npm run test:security:release-gate`: passed.
  - `npm run test:db:rls`: passed, 2 files / 26 tests; local reset applied current untracked taxonomy migrations because they exist in the worktree.
  - `npm run build`: passed on Next 16.2.9, 331 pages generated.
  - `npm run check:launch-test-coverage -- --require-complete`: passed; complete launch account coverage is yes.
  - `npm run check:algolia-search`: passed; 56 active Supabase ads matched 56 searchable Algolia records.
  - `npm audit --json`: passed with 0 vulnerabilities across 1069 dependencies.
- `npm run test:vercel-ppr-lambda-blocker-script`: passed 6/6; `npm run check:vercel-ppr-lambda-blocker`: passed with 0 partially-static routes and no `next-resume` PPR chain headers after the Vercel packaging remediation.
- `npx vitest run src/lib/vehicle-taxonomy/candidate-store.test.ts src/lib/vehicle-taxonomy/write-guard.test.ts src/lib/vehicle-taxonomy/public.test.ts src/lib/vehicle-taxonomy/candidates.test.ts src/lib/vehicle-taxonomy/autobazar-eu.test.ts src/lib/vehicle-taxonomy/mobile-de.test.ts src/lib/vehicle-taxonomy/otomoto.test.ts`: passed 7 files / 18 tests after adding the taxonomy write guard and promotion dry-run planner.
- `npm run typecheck`: passed after removing stale generated `.next/dev` artifacts, regenerating route types, and running TypeScript without stale incremental state.
- `npm run build`: passed with Next 16.2.6 after removing stale generated `.next/dev` artifacts before build.
- `npm run typecheck`: failed once on Next 16 App Router route extra exports, then passed after moving route helpers into sidecar modules.
- `npm run build`: passed after the route-helper export fix, including TypeScript and 1573 generated static pages.
- `npx vitest run src/app/api/contact/route.rate-limit.test.ts src/app/api/account/password/recovery/route.test.ts src/app/api/stripe/checkout/route.idempotency.test.ts src/app/api/stripe/checkout/route.rate-limit.test.ts src/app/api/stripe/webhook/route.test.ts src/app/api/admin/quality-gates/route.test.ts src/app/api/monitoring/quality-gates/route.test.ts --pool=forks --no-file-parallelism --maxWorkers=1`: passed 7/7 files and 62/62 tests.
- `npm run easy:quick`: passed with 89/89 unit files and 448/448 tests.
- `npm run test:security:release-gate`: passed after the homepage reflow, health-test updates, App Router route-helper policy update, and cleanup-aware typecheck command.
- `npm run check:i18n-contract`, `npm run check:sk-diacritics`, and `npm run check:i18n-diacritics`: passed after adding `bodyType.motorcycle` to Slovak, English, and Hungarian locale catalogs.
- `npx vitest run src/app/api/account/password/route.test.ts`: passed 7/7 tests.
- `npx vitest run src/app/api/account/password/route.test.ts src/app/api/account/password/recovery/route.test.ts src/app/api/auth/password-reset/route.security.test.ts src/app/api/auth/register/route.test.ts src/app/api/auth/register/resend/route.test.ts`: passed 40/40 tests.
- `npx vitest run src/app/api/account/password/recovery/route.test.ts`: passed 13/13 tests.
- `npx vitest run src/app/api/account/password/recovery/route.test.ts src/app/api/auth/password-reset/route.security.test.ts src/app/api/auth/register/route.test.ts src/app/api/auth/register/resend/route.test.ts`: passed 32/32 tests.
- `npx vitest run src/lib/security/maintenance-bypass.test.ts`: passed 8/8 tests.
- `npx vitest run src/lib/security/maintenance-bypass.test.ts src/app/api/maintenance/unlock/route.test.ts src/proxy.test.ts`: passed 27/27 tests.
- `npx vitest run src/app/api/health/route.test.ts src/proxy.test.ts src/lib/security/maintenance-bypass.test.ts`: passed 29/29 tests.
- `npx vitest run src/app/api/maintenance/unlock/route.test.ts`: RED failed because legacy `MAINTENANCE_PASSWORD` still unlocked maintenance; GREEN passed 6/6 after removing that alias.
- Safe Vercel Production and Preview env pulls showed the historical leaked maintenance value and legacy alias are not configured. Later metadata-only env-list checks show `MAINTENANCE_UNLOCK_PASSWORD` and `MAINTENANCE_BYPASS_SECRET` exist by name in both targets; temp env files were deleted, and live runtime smoke is still required because sensitive values cannot be read back through CLI.
- `.env.local`, `.vercel/`, and `.env.local.bak-20260322-221455` were confirmed ignored; the stale backup had 0 backup-only keys and was removed without printing values. Recheck showed only ignored `.env.local` and `.vercel/` remain.
- `npx vitest run src/lib/security/maintenance-bypass.test.ts src/app/api/maintenance/unlock/route.test.ts src/proxy.test.ts`: passed 32/32 after the maintenance alias removal.
- `git diff --check`, `npm run lint`, `npm run typecheck`, `npm run test:unit`, `npm run test:security:release-gate`, and `npm run build`: passed after the maintenance alias removal; unit tests passed 105 files / 508 tests and build generated 331 pages.
- `npm run check:theme-guard`: passed after normalizing the unrelated marketplace UI raw hex to existing background color tokens.
- `npx vitest run src/app/api/auth/register/route.test.ts src/app/api/auth/register/resend/route.test.ts src/app/api/auth/password-reset/route.security.test.ts`: passed 20/20 tests.
- `npx vitest run src/app/api/account/dealer-verification/route.test.ts`: passed 6/6 tests.
- `npx vitest run src/app/api/account/dealer-verification/route.test.ts src/app/api/account/ads/route.test.ts src/app/api/account/ads/apply-action/route.test.ts`: passed 17/17 tests.
- `npx vitest run src/app/api/account/ads/route.test.ts`: passed 12/12 after adding seller-owned delete, invalid-id/auth/ownership denial, DB delete failure, and Algolia cleanup-failure-before-DB-delete coverage.
- `npx vitest run src/lib/analytics/events.test.ts`: passed 17/17 after adding the `listing_deleted` analytics taxonomy event.
- `npx vitest run src/app/api/billing/checkout-status/route.test.ts`: passed 8/8 tests.
- `npx vitest run src/app/api/billing/checkout-status/route.test.ts src/app/api/stripe/checkout/route.behavior.test.ts src/app/api/stripe/checkout/route.idempotency.test.ts src/app/api/stripe/checkout/route.rate-limit.test.ts src/app/api/stripe/webhook/route.test.ts`: passed 42/42 tests.
- `npx vitest run src/app/api/stripe/checkout/route.behavior.test.ts src/app/api/stripe/checkout/route.idempotency.test.ts src/app/api/stripe/checkout/route.rate-limit.test.ts src/app/api/stripe/webhook/route.test.ts`: passed 33/33 tests.
- `npx vitest run src/app/api/stripe/webhook/route.test.ts src/lib/email/jobs.test.ts`: passed 28/28 after payment failure email queueing and nullable failure transaction ids.
- `npx vitest run src/app/api/stripe/checkout/route.behavior.test.ts`: passed 5/5 after checkout fail-closed handling for Stripe session id update failure.
- `npx vitest run src/app/api/billing/checkout-status/route.test.ts src/app/api/stripe/checkout/route.behavior.test.ts src/app/api/stripe/checkout/route.idempotency.test.ts src/app/api/stripe/checkout/route.rate-limit.test.ts src/app/api/stripe/webhook/route.test.ts src/lib/email/jobs.test.ts`: passed 49/49 after the checkout fail-closed fix.
- `npm run lint`, `npm run typecheck`, `npm run test:security:release-gate`, and `npm run build`: passed after the checkout fail-closed fix; build generated 1574 pages.
- `npx vitest run src/app/api/stripe/webhook/route.test.ts`: passed 26/26 after paid checkout billing-apply failures were changed to return `500` for Stripe retry.
- `npx vitest run src/app/api/billing/checkout-status/route.test.ts src/app/api/stripe/checkout/route.behavior.test.ts src/app/api/stripe/checkout/route.idempotency.test.ts src/app/api/stripe/checkout/route.rate-limit.test.ts src/app/api/stripe/webhook/route.test.ts src/lib/email/jobs.test.ts`: passed 51/51 after the paid-webhook retry fix.
- `git diff --check`, `npm run lint`, `npm run typecheck`, `npm run test:security:release-gate`, and `npm run build`: passed after the paid-webhook retry fix; build generated 1574 pages.
- `npm run test:db:rls -- supabase/tests/billing-checkout-atomicity.test.sql`: passed 1 file / 2 tests after Docker Desktop was recovered.
- `npm run test:db:rls`: passed 2 files / 26 tests after the SQL atomicity migration/test.
- `npm run test:db:rls`: passed 2 files / 26 tests during the live Supabase RLS audit.
- Live anon Supabase probe: `npm run check:live-rls-posture -- --json` failed with `ok=false`, 4 leaked probes, 0 probe errors, and 1 anon-readable row for `profiles.email`, `profiles.phone`, `profiles.credit_balance`, and raw `dealers`; no row values were printed.
- `npx vitest run src/lib/cars/public-car-detail.test.ts`: passed 2/2 after adding the RLS-compatible public listing detail helper.
- Real local Stripe checkout creation preflight: local Stripe key was test-mode; an authenticated seller browser session called the real `/api/stripe/checkout` endpoint for `private_listing_action` / `prolong_top`; the endpoint returned 200; Stripe created an unpaid payment Checkout Session; `billing_checkout_sessions` stored a matching `created` row; cleanup expired the test session, removed matching checkout/idempotency rows, restored the seller ad fixture, and saw 0 browser/page console errors.
- Real local paid Stripe smoke: Docker `stripe/stripe-cli` forwarded test-mode Stripe events while a seller paid Checkout with card `4242 4242 4242 4242`; the payment redirected to `/platba/uspech`, `billing_checkout_sessions.status` became `paid`, 1 `billing_transactions` row was created, and the `prolong_top` listing action was applied before cleanup restored the seller fixture and removed matching test rows. No `payment_confirmation` email job was observed; the delivered event had customer email and billing metadata, but the stored webhook log came from the currently deployed/older webhook path.
- `npx vitest run src/app/api/billing/checkout-status/route.test.ts src/app/api/stripe/checkout/route.behavior.test.ts src/app/api/stripe/checkout/route.idempotency.test.ts src/app/api/stripe/checkout/route.rate-limit.test.ts src/app/api/stripe/webhook/route.test.ts src/lib/email/jobs.test.ts`: passed 52/52 after the payment confirmation email fallback transaction lookup.
- `git diff --check`, `npm run lint`, `npm run typecheck`, `npm run test:security:release-gate`, and `npm run build`: passed after the payment confirmation fallback hardening; build generated 331 pages.
- `git diff --check`, `npm run lint`, `npm run typecheck`, `npm run test:unit`, `npm run test:security:release-gate`, and `npm run build`: passed after the public detail compatibility fix; unit tests passed 105 files / 507 tests and build generated 331 pages.
- `npm run test:release-gauntlet`: passed 8/12 checks with 4 honest skips after dependency hardening and Playwright `.env.local` runner loading was fixed.
- `PLAYWRIGHT_CHROMIUM_CHANNEL=chrome npx playwright test tests/release-gauntlet.test.ts --project=desktop-chromium --reporter=line`: passed 18/18 on 2026-06-20, including seller create, two-photo upload, edit description/price, photo removal, mark sold, seller dashboard delete/remove, non-owner edit-page denial, dashboard create-tab single `h1`, real recovery-token password reset, password restore, admin dealer-verification request visibility with a cleaned-up temporary fixture, buyer inquiry submit through seller dashboard read, and cleanup.
- `PLAYWRIGHT_CHROMIUM_CHANNEL=chrome PLAYWRIGHT_REUSE_SERVER=false npx playwright test tests/release-gauntlet.test.ts --project=desktop-chromium --reporter=line --grep "buyer inquiry"`: passed 1/1 on 2026-06-20 as the focused buyer inquiry check, including submit, DB row verification, seller dashboard read, and cleanup.
- 2026-06-20 real auth delivery: queued Resend processing sent signup confirmation and password reset emails; Gmail received both expected subjects, delivery logs show sent rows, pending auth email jobs were 0, and temporary launch-test users were cleaned up.
- `npx vitest run src/lib/email/react-email-templates.test.ts src/lib/email/jobs.test.ts src/lib/email/transactional-email.test.ts src/app/api/auth/register/route.test.ts src/app/api/auth/register/resend/route.test.ts src/app/api/auth/password-reset/route.security.test.ts`: passed 6 files / 33 tests after fixing password-reset email detail-row spacing.
- 2026-06-20 real auth-link browser/Gmail check: initial RED confirmed raw Supabase signup action links landed on `/auth/auth-code-error`; after switching signup/resend emails to app `token_hash&type=email` callbacks, signup submit, Gmail confirmation delivery, confirmation-link login to `/moj-ucet`, password-reset request, Gmail reset delivery, reset-link password update, old-password rejection, new-password browser login, temp-user cleanup, and pending auth email jobs 0 all passed.
- `npx vitest run src/app/auth/callback/route.test.ts src/app/api/auth/register/route.test.ts src/app/api/auth/register/resend/route.test.ts src/app/api/auth/password-reset/route.security.test.ts src/app/api/account/password/recovery/route.test.ts`: passed 5 files / 38 tests after the auth-link callback fix.
- `npm run typecheck`, `npm run lint`, `npm run test:security:release-gate`, `git diff --check`, and `npm run build`: passed after the auth-link callback fix; build generated 331 pages.
- `PLAYWRIGHT_CHROMIUM_CHANNEL=chrome PLAYWRIGHT_REUSE_SERVER=true npx playwright test tests/release-gauntlet.test.ts --project=desktop-chromium --reporter=line --grep "dealer"`: passed 3/3 after adding admin dealer-verification request visibility coverage.
- Temporary dealer-verification fixture cleanup probe: `release_gauntlet_dealer_verification_rows=0`.
- `npm run check:launch-test-coverage -- --require-complete`: passed as a read-only coverage report; complete launch test account coverage is yes for primary/admin, non-admin, seller-with-owned-ad, and dealer coverage.
- `npm run test:launch-test-coverage-script`: passed 2/2 tests for launch-account checker role fallback and candidate counting.
- `npm run check:algolia-search`: passed as a read-only real index check; 56 active Supabase ads matched 56 searchable Algolia records.
- `npm run test:algolia-search-script`: passed 3/3 tests for Algolia coverage-checker validation logic.
- `npx vitest run src/app/sitemap.test.ts src/lib/seo/programmatic-taxonomy.test.ts src/lib/seo/inventory.test.ts 'src/app/(site)/[brand]/[model]/[city]/page.test.tsx' 'src/app/(site)/[brand]/[model]/page.test.tsx' 'src/app/(site)/vysledky/SearchSeoLinks.test.tsx'`: passed 22/22 after pSEO launch gating.
- `npm run test:seo-taxonomy`: passed 30/30 after pSEO launch gating.
- `npm run lint`, `npm run typecheck`, `git diff --check`, and `PLAYWRIGHT_CHROMIUM_CHANNEL=chrome npm run test:web-interface`: passed after pSEO launch gating; web-interface passed 18/18.
- `npm run build`: passed after pSEO launch gating and generated 331 pages instead of the earlier 1574-page build.
- `npm run check:sk-diacritics`, `npm run check:i18n-contract`, `npm run check:i18n-diacritics`, `npm run lint`, `npm run typecheck`, `git diff --check`, `npm run build`, and `PLAYWRIGHT_CHROMIUM_CHANNEL=chrome npm run test:web-interface`: passed after public copy overclaim cleanup; build generated 331 pages and web-interface passed 18/18.
- `npx vitest run src/app/sitemap.test.ts src/app/robots.test.ts src/app/llms.txt/route.test.ts src/lib/auth/request-origin.test.ts src/lib/security/csrf.test.ts`: passed 5 files / 21 tests after canonical host alignment to `https://www.autobazar123.sk`.
- `npx vitest run src/app/sitemap.test.ts src/app/robots.test.ts src/app/llms.txt/route.test.ts src/lib/seo/programmatic-taxonomy.test.ts src/lib/seo/inventory.test.ts 'src/app/(site)/[brand]/[model]/[city]/page.test.tsx' 'src/app/(site)/[brand]/[model]/page.test.tsx' 'src/app/(site)/vysledky/SearchSeoLinks.test.tsx'`: passed 8 files / 25 tests after `seo.config.md` primary-domain cleanup.
- `npm run test:seo-taxonomy`: passed 3 files / 30 tests after `seo.config.md` primary-domain cleanup.
- Fresh 2026-06-21 SEO refresh: live read-only probe confirmed `https://autobazar123.sk/` returns 307, `https://www.autobazar123.sk/` returns 200 with noindex header/meta and apex canonical, `/robots.txt` disallows all crawlers, `/sitemap.xml` has 1389 apex-host `<loc>` entries, `/llms.txt` primary URLs use apex, and `/api/health` returns healthy. `npm run test:seo-taxonomy` passed 3 files / 30 tests; `npx vitest run src/app/sitemap.test.ts src/app/robots.test.ts src/app/llms.txt/route.test.ts` passed 3 files / 9 tests.
- `npm run lint`, `npm run typecheck`, `git diff --check`, and `npm run build`: passed after canonical host alignment; build generated 331 pages.
- `npx vitest run src/app/api/cron/expire-ads/route.test.ts src/lib/fallbacks/registry.test.ts`: passed 4/4 tests.
- `npx vitest run src/app/api/cron/expire-ads/route.test.ts src/lib/fallbacks/registry.test.ts src/lib/env.test.ts`: passed 6/6 tests.
- `npx vitest run src/app/api/cron/send-alerts/route.test.ts src/app/api/cron/expire-ads/route.test.ts src/lib/fallbacks/registry.test.ts src/lib/env.test.ts`: passed 8/8 tests.
- `npx vitest run src/app/api/cron/process-email-jobs/route.test.ts src/app/api/cron/send-alerts/route.test.ts src/app/api/cron/expire-ads/route.test.ts src/lib/fallbacks/registry.test.ts src/lib/env.test.ts`: passed 10/10 tests.
- `npx vitest run src/app/api/cron/cleanup-sold/route.test.ts src/app/api/cron/process-email-jobs/route.test.ts src/app/api/cron/send-alerts/route.test.ts src/app/api/cron/expire-ads/route.test.ts src/lib/fallbacks/registry.test.ts src/lib/env.test.ts`: passed 14/14 tests.
- `npx vitest run src/lib/email/jobs.test.ts src/app/api/cron/process-email-jobs/route.test.ts src/app/api/cron/cleanup-sold/route.test.ts src/app/api/cron/send-alerts/route.test.ts src/app/api/cron/expire-ads/route.test.ts src/lib/fallbacks/registry.test.ts src/lib/env.test.ts`: passed 16/16 tests.
- `npx vitest run src/lib/email/jobs.test.ts src/lib/email/transactional-email.test.ts`: passed 4/4 after adding queued-email provider idempotency coverage.
- `npx vitest run src/lib/email/jobs.test.ts src/lib/email/transactional-email.test.ts src/lib/email/react-email-templates.test.ts src/app/api/cron/process-email-jobs/route.test.ts`: passed 15/15.
- `git diff --check`, `npm run lint`, `npm run typecheck`, `npm run test:security:release-gate`, and `npm run build`: passed after the queued-email idempotency fix; build generated 1574 pages.
- `npm run list:fallbacks`: passed with 9 registered fallbacks.
- `npm run check:algolia-search`: passed after the expire-ads cleanup fallback change; 56 active Supabase ads matched 56 searchable Algolia records.
- `npm run lint`, `npm run typecheck`, `npm run test:security:release-gate`, and `npm run build`: passed after the email job processor state-update fix; build generated 1574 pages.
- `PLAYWRIGHT_CHROMIUM_CHANNEL=chrome npm run test:web-interface`: passed 18/18 after the dashboard create-tab single-`h1` coverage check.
- `npx playwright test tests/reflow-zoom.test.ts`: passed 21/21 after the homepage reflow fix.
- `PLAYWRIGHT_CHROMIUM_CHANNEL=chrome npm run test:a11y`: passed 63/63 after the dashboard create-tab single-`h1` coverage check.
- `PLAYWRIGHT_CHROMIUM_CHANNEL=chrome npm run test:keyboard`: passed 9/9.
- `PLAYWRIGHT_CHROMIUM_CHANNEL=chrome npm run test:mobile-matrix`: passed 42/42.
- `PLAYWRIGHT_CHROMIUM_CHANNEL=chrome npm run test:ui-quality-gate`: passed after the dashboard create-tab single-`h1` coverage check, including 18/18 Playwright checks plus 19/19 UI unit tests.
- `npx vitest run src/context/AuthContext.test.tsx`: passed 1/1 after adding non-admin `site_admins` `.maybeSingle()` coverage.
- `node output/playwright/launch-screenshots/capture-launch-screenshots.mjs`: passed 18 desktop/mobile launch screenshots with 0 failed statuses, 0 console messages, 0 page errors, 0 network failures, 0 horizontal-scroll issues, and 0 too-wide elements.
- `PLAYWRIGHT_CHROMIUM_CHANNEL=chrome npx playwright test tests/web-interface-guidelines.test.ts tests/web-interface-sitewide.test.ts --reporter=line`: passed 18/18 after the launch screenshot/UI fixes.
- `PLAYWRIGHT_CHROMIUM_CHANNEL=chrome npx playwright test tests/accessibility-gate.test.ts tests/reflow-zoom.test.ts --reporter=line`: passed 63/63 after the launch screenshot/UI fixes.
- `PLAYWRIGHT_CHROMIUM_CHANNEL=chrome TEST_URL=http://localhost:3000 npx playwright test tests/keyboard-navigation.test.ts --reporter=line`: passed 9/9 after the launch screenshot/UI fixes.
- `PLAYWRIGHT_CHROMIUM_CHANNEL=chrome TEST_URL=http://localhost:3000 npx playwright test tests/accessibility-gate.test.ts tests/reflow-zoom.test.ts --project=mobile-pixel-7 --project=mobile-iphone-13-landscape --reporter=line`: passed 42/42 after the launch screenshot/UI fixes.
- `PLAYWRIGHT_CHROMIUM_CHANNEL=chrome TEST_URL=http://localhost:3000 npm run test:ui-quality-gate`: passed after the launch screenshot/UI fixes.
- `npm run typecheck`, `npm run lint`, `npm run test:unit`, `npm run test:security:release-gate`, and `npm run build`: passed after the launch screenshot/UI fixes; unit tests passed 104 files / 505 tests and build generated 331 pages.
- `git diff --check`: passed.
- Focused Playwright runtime check for desktop and mobile `/vysledky?bodyStyle=motorcycle`: passed with status 200, 0 console issues, and 0 network issues.
- Latest full `PLAYWRIGHT_CHROMIUM_CHANNEL=chrome npm run audit:webapp`: passed on 2026-06-19. Playwright completed 5/5 tests and the saved report at `output/playwright/webapp-audit.json` records `complete: true`, 80 route/viewport checks, 0 failing routes, 0 console warnings/errors, 0 network failures, and 0 DevTools issues.
- `tests/webapp-audit.ts` now writes partial JSON reports, supports `AUDIT_VIEWPORT` / `AUDIT_ROUTE_OFFSET`, applies the intended long timeout, detaches CDP sessions, and isolates browser contexts per route. After the dev-artifact/server cleanup fixes, the full local audit completes successfully.
- Local `next start` performance validation after `npm run build`: passed when the audit process received a secret-safe temporary Upstash env from the old local backup. Temporary Preview/Production Vercel env pulls are deleted after masked checks. The updated checker treats `UPSTASH_REDIS_REST_TOKEN` as Vercel sensitive metadata rather than a pull-readable value, so local env pull/readability is no longer a blocker; cloud runtime smoke remains required.

## Remaining Launch Blockers

- Public SEO remains launch-blocked until the owner explicitly approves indexing/opening; indexing must stay disabled until final robots, sitemap, canonical/`www`, llms, metadata, and Search Console readiness checks pass.
- Deployed maintenance-bypass runtime smoke passed; re-smoke after any maintenance env/proxy change.
- Deployed cron route smoke passed; actual Vercel Cron scheduler behavior still needs Production log review after scheduled invocations.
- Failed-payment provider smoke passed for seller listing action; broader dealer topup payment variants remain open.
- Broader deployed authenticated browser flow status: Production release gauntlet passed 17/18. Auth/dashboard/signout, password recovery token, admin/non-admin/dealer gates, seller paid action, dealer topup payload, admin dealer-verification visibility, dashboard edit/top/sold controls, non-owner denial, and seller create/edit/photo-remove/mark-sold/delete all passed. Buyer inquiry failed because Turnstile is not configured in Production. Real Cloudflare upload remains separate because the gauntlet mocks the upload provider.
- Fresh local Docker-backed `npm run test:db:rls` passed from clean reviewed source `2297260`; rerun after any future DB/RLS migration change.
- Cloud runtime/provider smoke is still useful for sensitive values such as Upstash, even though route smoke, maintenance smoke, cron route smoke, and payment success/failure proved important deployed env paths.
- Dirty main still contains the separate taxonomy lane; do not push/deploy/migrate from dirty main without explicit source-choice review.
- User approval is required before contacting dealers.
