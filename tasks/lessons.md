# Lessons Learned

- Avoid hard-coded colors and rely on theme tokens for all surfaces, borders, and text.
- Keep input font size at text-base (16px) on mobile to prevent zoom and improve readability.
- Replace transition-all with explicit transition properties for clarity and performance.
- Add min-w-0 and truncate on filter rows to prevent overlap between long labels and counts.

