# Active Todo

No open tasks right now.

## Review

- Repo-wide `react-doctor` baseline is now clean: `0 errors / 0 warnings`.
- Verification command: `npx react-doctor --yes --offline` (clean tree, 191 source files scanned).
- Added `knip.json` entrypoints so the Cloudflare worker entry file is treated as a valid project entry.
